# Browser Bridge Agent 协作准则

适用于 `cnjimmyshao/fjzx.browser-bridge` 的编码、Review、自动修复、调度及其委派的子 Agent。

**以最简单的正确实现完成当前能力。对业务结果严格，对实现复杂度克制；防御措施与实际风险相称。不要把逐条消除 Review 评论或封堵所有理论上的错误用法，当作项目目标。**

## 文档与术语

文档、Issue、PR、Review 和 Agent 交接采用**中文叙述 + 英文专业术语**。普通解释保留自然中文；表达明确的技术概念或项目专有名称时使用统一英文，不逐字英文化。

- **Contract**：约定的接口、数据语义或行为保证；**Schema**：数据结构及声明式约束，不等于完整业务 Contract。
- **Requirement**：需要实现的需求；**Scope**：当前任务包含与不包含的范围；**Implementation**：具体实现方式。不要把某一种 Implementation 自动升级为 Requirement 或扩大 Scope。
- **Review**：代码审查；**Finding**：Review 发现项，是否成立及是否阻塞仍需核实；**Audit**：另有明确范围的审计，不作为 Review 的同义词。

首次出现的生僻或有歧义术语给出简短中文定义。同一技术概念统一命名，例如技术文档中的“合同/合约/契约”统一为 Contract；普通叙述仍可使用“需求、范围、实现”等中文。项目专有名称沿用相应 Contract 和代码中的英文。

代码标识符、字段名、API、命令、文件路径及其大小写保持原样，不因术语整理改名；日志原文、引用和历史证据不改写。修改标题时保留已有链接锚点或同步相关引用。术语整理只统一表达，不改变业务含义、Contract、Scope 或验收标准。

## 开始任务与权威来源

每次开始编码、审查、恢复任务或修复前，实际读取本文件、[架构与协议](docs/architecture-v1.md)、[README](README.md) 中与任务相关的内容，以及当前 Issue/PR 的维护者决定。不要假设其他会话已经传递了上下文。

本文件规定工程方法，不取代架构、协议和已确认的验收要求。任务范围由 Issue 及已确认决定确定；探索材料、旧方案和 Review 建议不能自行升级为新需求。发现支持范围、职责或接口语义冲突时，在当前 Issue/PR 提出具体冲突与最简单方案，由维护者决定；不受影响的工作可以继续。已确认的新决定同步到对应权威文档。

不得为通过审查而降低验收标准、删除有效测试、隐瞒风险，或把本来承诺支持的操作改称“不支持”。本文件也不授权直接删除已有协议、状态或权限保护；需要迁移职责或改变承诺时，说明替代路径、影响和测试，取得必要决定后再同步实现与文档。

## Browser Bridge 的设计边界

以下是当前 V1 的工作基线，不是阻止未来经批准变更的永久禁令；细节及后续变更以架构文档和已确认任务为准。

- Bridge 是独立、通用、轻量、稳定、无网站业务语义的 Extension。Service 负责浏览器进程、Profile、Proxy、调度和业务数据；Bridge 负责浏览器内部连接、Work Tab、当前 Job、受控执行和技术结果返回。
- 当前采用唯一 Work Tab、同一时间一个 Job；未就绪或忙碌时如实返回技术状态。不要为尚未承诺的多 Tab/并行场景预建协调系统，也不能把单 Job 误当成没有异步竞态。
- 每次 EXECUTE 直接携带 Service JavaScript，在 Work Tab 主框架的 USER_SCRIPT world 中执行。页面 DOM 读取、点击、输入、滚动等由 Service Script 完成，不逐项包装成 Bridge 业务 API。
- Bridge 不解释网站、Selector、Action、Quota、登录、验证码、风控或业务 BLOCKED。`RESULT.ok` 表示技术执行是否成功，不代表业务成功；不要为完善业务流程而把这些职责搬进 Bridge。
- 当前 V1 不建设 Runtime 下载/缓存/版本框架、平台 Adapter、MAIN world、通用 Chrome API 转发、Job Queue/History、Job Retry/幂等或 Exactly Once。连接恢复与 Job 重试是不同责任，不能因为允许重连就自动重放有副作用的脚本。
- 已确认的最小 Browser Capability 可按对应任务实现；不要将“无业务语义”误解为永远不能新增通用能力。当前 Service URL 配置与访问取舍也不自动升级为新认证、绑定或部署系统；实际可达风险仍应给出证据并处理。

## 实现原则

- **能力优先。** 先使正常连接、执行、状态查询和结果返回清楚、完整、可用，再覆盖相关异常。既验证不该发生的事情被阻止，也验证合法操作没有被防御代码误伤。
- **整体简单。** 选择理解、测试和维护成本最低的方案，不只追求改动行数少。简单函数足够时不增加架构层；不为尚未承诺的场景预建框架、适配层、复杂状态机、自动恢复或性能优化。
- **责任明确。** 区分协议结构校验、技术执行、生命周期、并发和安全边界。优先在真正负责该行为的模块解决规则，不把 Service 的业务保证转移给 Bridge，也不让底层模块为任意调用组合无限兜底。必要的协议校验、执行隔离和相关并发控制仍应保留。
- **风险相称。** 分开分析受控内部调用与外部输入，不预设所有内部调用者都会刻意绕过规则。Service 消息、网页、脚本执行、Extension 权限和诊断数据等实际边界仍须核实；能发来消息不等于输入已合法。低概率但严重且可达的风险仍须处理。
- **尊重取舍。** 按已确认协议理解技术失败、不可用状态、结果不持久化以及由 Service 决定的恢复行为。不得因追求绝对防误用、数据整齐或未来扩展自动增加保证，也不能把未确认假设当成已接受限制。运行方式、恢复策略、兼容范围和性能目标不擅自升级。
- **验证结果。** 用适量测试覆盖实际支持的能力、关键异常和相关异步竞态。通过真实入口验证行为，不只测试内部 helper；可复现缺陷的回归测试应能区分旧实现与修复。测试同时包含成功路径和必要的拒绝路径。

## Code Review Rules

### 范围与证据

审查改动是否以足够简单的方式正确实现当前能力，而不是比较谁能想出更多理论反例。缺陷意见应说明：运行前提、实际可达或已承诺支持的路径、违反的约束、具体后果。清楚的代码分析可以构成证据，不要求问题先在线上发生；缺少当前调用方也不能否定公开协议或外部可达风险。

区分真实缺陷、设计质疑、可选增强和个人偏好。质疑已确认取舍时，说明哪个前提不成立及其后果；不要把架构偏好、新需求或未承诺的保证包装成必须修复的 bug。真实的正确性、安全性、兼容性或正常运行故障不能因“简单优先”而被降级或忽略。不限制 Finding 数量，不伪造无问题结论。

### 根因与复杂度

同一根因在多个路径出现时尽量归并，指出共同责任层和受影响路径。既检查遗漏和缺陷，也检查重复规则、职责错位、补丁叠加、正常能力被拦截和不必要的通用化。复杂度问题同样要有具体影响，不能变成另一种风格争论。

局部修复足够就直接修复；需要统一处理时，不要逐个反例追加特判。横向检查与根因相关的路径，不借机要求无关重构。不要因“防御性代码”这一标签默认接受复杂实现，也不要为了变短删除必要保证。

### 协议与完成标准

先核对本任务涉及的架构、协议、运行前提及维护者决定。技术约束不自动等于某一种实现机制；当前文档明确要求的机制若需调整，应指出迁移需要的决定，而不是忽略文档或继续无限增强。尊重 Bridge、Service 和 Service Script 的职责分配。

机械格式检查留给对应工具；非阻塞建议不自动变成合并条件。Review 无 Finding 不等于自动测试、真实浏览器验证或业务验收已完成。达到当前任务验收后停止扩展，不把整个项目未来的能力都加到当前 PR。

## 验证与交付

当前验证入口以 [package.json](package.json) 和 README 为准：

- `npm test`：Node 单元与集成测试，当前等价于 `node --test`。
- `npm run poc`：真实浏览器端到端 POC；环境准备按 README，使用独立测试 Profile 和本地测试页面。

按修改范围选择验证。纯文档改动检查引用、约束一致性和 diff，不无条件重跑浏览器 POC；涉及执行 world、权限、Service Worker 生命周期或 Chrome API 行为的修改，需要相应真实浏览器证据，不能用 mock 通过代替。明确记录环境、已运行结果及未运行项目，不伪装 PASS；不依赖用户的日常 Profile 或真实业务账号做自动验收。

核实 Review 意见后按共同根因修复、补相关测试并同步必要文档，不按 P1/P2/P3 标签机械增加代码。同类问题多轮反复或补丁持续增加例外时，先重新审视职责和实现；需要维护者决定就提出具体方案，不无限重试，也不靠忽略真实缺陷收尾。

同一 PR 分支的写入由一个执行者协调。一轮相关代码、测试和文档完成后统一送审，不逐个中间提交催审；正在审查时不重复触发，结果核对 reviewed commit 与当前 head。基础设施失败先诊断，不因浏览器环境或审查服务问题乱改业务代码，也不把失败当作审查通过。

交付简要说明实现的能力、共同根因、必要改动理由、实际验证和剩余限制；简单任务几句话即可，不额外要求设计论文。没有明确授权，不自动 merge、部署、操作用户浏览器环境或删除真实数据。

## 各类 Agent 的接入

本文件是本仓库共享工程原则的单一维护位置；Issue、PR 和启动提示引用它，不复制多套可能冲突的规则。支持自动加载 `AGENTS.md` 的工具使用该入口；其他 Agent/runner 在启动时显式读取并传入本文件内容。委派子任务时传递同一规则版本、相关协议及本次范围。

任务启动提示：

> 开始前读取本仓库 AGENTS.md（包含 Code Review Rules）、相关架构与协议及本任务的维护者决定。以最简单的正确实现完成当前能力，先核实范围与共同根因，再编码或审查。简要标明本次所用仓库/ref 与规则来源，不只回复“已理解”。

规则更新后，已有会话、旧分支和不自动加载文件的 runner 需要显式重读或更新上下文。文件存在不代表所有参与者已经收到；尚未合并的规则只覆盖已包含该文件或被明确传入该版本的任务，不宣称 main 和所有外部 Agent 已自动生效。
