# Browser Bridge Agent 协作准则

适用于 `cnjimmyshao/fjzx.browser-bridge` 的编码、Review、自动修复、调度及其委派的子 Agent。

**以最简单的正确实现完成当前能力。对业务结果严格，对实现复杂度克制；防御措施与实际风险相称。不要把逐条消除 Review 评论或封堵所有理论上的错误用法，当作项目目标。**

## 开始任务与权威来源

每次开始或恢复任务，包括规划、文档整理、编码、审查和修复，实际读取本文件、[Current](docs/current/README.md) 中相关的 Contract、关联 Issue 正文及相关评论中的问题与已确认决定，以及已有 PR 的 Review / 修复记录。不要只看正文，也不要假设其他会话已经传递了上下文。

本文件规定工程方法，不取代架构、协议和验收要求。任务 Scope 由 Issue 及已确认决定确定；探索材料、旧方案和 Review 建议不能自行升级为新需求。已确认的新决定应落实到相应权威文档，冲突按「决策交接与完成标准」处理。

### Issue → PR 开发入口

**所有仓库内容的变更，包括纯文档、注释和格式维护，都先关联已有或新建 Issue，再走开发分支 → PR → Review → 获得合并授权后 Merge → 核实 Issue 收尾。不得以“只是文档”或“改动很小”为由直接修改 main 或绕过 Review。**

已有对应 Issue 时直接使用，不为每个修订另开重复任务。先写清范围与可观察的验收结果，再修改分支；必要时先做范围明确的 Research。工具具有写入、Review 或 Merge 能力，不等于本次任务已获得这些授权。

### 角色与职责

角色按任务使命与授权确定，不绑定模型、主机或工具；同一 Agent 可兼任，但须区分自查与独立 Review，不要求新增 Agent 或逐阶段人工审批。

| 角色 | 责任与边界 |
| --- | --- |
| Coordinator（规划／协调者） | 读取依据、编写与拆分 Issue、整理待决问题、记录已确认决定及交接；建议不是批准，不因主持讨论获得维护者决策权。 |
| Implementer（实现者） | 在已确认范围内自主选择普通实现，完成代码、测试及必要文档，提供可验证的交付。 |
| Reviewer（审查者） | 核对当前 head 的正确性、证据、Contract 与复杂度；不把个人修法偏好变成需求，不因有写权限自行修复或合并。 |
| Maintainer（维护者） | 决定 Scope、技术语义、保障范围、重要取舍和合并放行；由人或明确授权的代理在授权范围内承担。 |

只读观察不自行升级为修改；委派子任务不扩大原有授权。以下共同规则也适用于文档作者、协调者和调度者，不只适用于写代码的 Agent。

### 编写与拆分 Issue

目标是让新接手者不依赖历史聊天或长启动提示，就能找到依据、理解边界、开始工作并判断完成。

- 作者先实际读取依据，核对文件、章节和决定评论，再说明「读什么、为什么读」。区分 Current、已确认决定与待研究材料，不只写“参考相关文档”，不机械要求通读全仓库。
- 写清问题或目标结果、Scope，以及容易混淆时的非目标；依赖、环境、权限和特殊基线按需交代，不把单次实验条件推广为默认限制。
- 区分 Requirement 与 Implementation。可以给分析或推进建议，但不要预先规定未经确认的实现方式；普通技术取舍留给 Implementer。
- 影响接口、职责、保障范围或验收的未决事项显式保留，不替维护者猜成确定需求。理论风险、未来扩展或某个 API 的存在，不自动成为本次任务。
- 验收说明通过什么操作及可观察结果判断目标达成；提交 PR、测试通过或 Review 状态不能单独代替目标。只在子任务可独立执行和验收时拆分，不机械制造大量 Issue。

这些是信息质量要求，不是固定标题、篇幅或模板。小任务几句话即可；引用已有文档和决定，不复制整套长期规则。

### 文档与实现同步

文档分类与权威见 [docs/README.md](docs/README.md)。始终从 `docs/current/` 读取当前 Contract，不按版本号寻找或创建另一套当前目录；Current Version 由维护者决定，普通修复、重构、测试或文档整理不自行升级版本。

开始工作先核对代码、测试、Current 与已确认决定。长期协议、状态机、职责、执行限制或保障发生批准后的变化时，同一 PR 同步 Current；重要且易被重新争论的取舍记入 Decision；新的浏览器实测形成带环境和证据的 Research。若只是补齐已准确描述的行为、等价重构或修复偏离，则核对一致性即可，不为制造文档 diff 而改写 Contract。

代码与测试描述当前实现，不得用旧实现反向降低承诺；Research 可以质疑 Current，但不能自动覆盖它。发现旧文档的真实冲突，记录依据、影响和待决范围，不借整理文档暗中选择协议。Review 同时检查文档、代码和测试的必要同步。

不得为通过审查而降低验收标准、删除有效测试、隐瞒风险，或把本来承诺支持的操作改称“不支持”。本文件不授权直接删除已有协议、状态或权限保护；迁移职责或改变承诺时，说明替代路径、影响和测试，取得必要决定。

## Browser Bridge 的设计边界

以下是当前工作的基线，不是阻止未来经批准变更的永久禁令；具体 Contract 从 [Current](docs/current/README.md) 读取。

- Bridge 是独立、通用、轻量、稳定、无网站业务语义的 Extension。Service 负责浏览器进程、Profile、Proxy、调度和业务数据；Bridge 负责浏览器内部连接、Work Tab、当前 Job、受控执行和技术结果返回。
- 当前采用唯一 Work Tab、同一时间一个 Job；未就绪或忙碌时如实返回技术状态。不要为尚未承诺的多 Tab/并行场景预建协调系统，也不能把单 Job 误当成没有异步竞态。
- 每次 EXECUTE 直接携带 Service JavaScript，在 Work Tab 主框架的 USER_SCRIPT world 中执行。页面 DOM 读取、点击、输入、滚动等由 Service Script 完成，不逐项包装成 Bridge 业务 API。
- Bridge 不解释网站、Selector、Action、Quota、登录、验证码、风控或业务 BLOCKED。`RESULT.ok` 表示技术执行是否成功，不代表业务成功；不要为完善业务流程而把这些职责搬进 Bridge。
- 当前不建设 Runtime 下载/缓存/版本框架、平台 Adapter、MAIN world、通用 Chrome API 转发、Job Queue/History、Job Retry/幂等或 Exactly Once。连接恢复与 Job 重试是不同责任，不能因为允许重连就自动重放有副作用的脚本。
- 已确认的最小 Browser Capability 可按对应任务实现；不要将“无业务语义”误解为永远不能新增通用能力。Service URL 配置与访问取舍也不自动升级为新认证、绑定或部署系统；实际可达风险仍应给出证据并处理。

## 实现原则

- **能力优先。** 先使正常连接、执行、状态查询和结果返回清楚、完整、可用，再覆盖相关异常。既验证不该发生的事情被阻止，也验证合法操作没有被防御代码误伤。
- **整体简单。** 选择理解、测试和维护成本最低的方案，不只追求改动行数少。简单函数足够时不增加架构层；不为尚未承诺的场景预建框架、适配层、复杂状态机、自动恢复或性能优化。
- **责任明确。** 区分协议结构校验、技术执行、生命周期、并发和安全边界。明确某条规则由调用方遵守，还是由模块在约定范围强制保障；不得仅凭“必须／不得”推导为底层需防御所有调用组合。不要把 Service 的业务保证转移给 Bridge，必要的协议校验、执行隔离和并发控制仍须保留。
- **风险相称。** 分开分析受控内部调用与外部输入，不预设所有内部调用者都会刻意绕过规则。Service 消息、网页、脚本执行、Extension 权限和诊断数据等实际边界仍须核实；能发来消息不等于输入已合法。低概率但严重且可达的风险仍须处理。
- **尊重取舍。** 按已确认协议理解技术失败、不可用状态、结果不持久化以及由 Service 决定的恢复行为。不得因追求绝对防误用、数据整齐或未来扩展自动增加保证，也不能把未确认假设当成已接受限制。运行方式、恢复策略、兼容范围和性能目标不擅自升级。
- **验证结果。** 用适量测试覆盖实际支持的能力、关键异常和相关异步竞态。通过真实入口验证行为，不只测试内部 helper；可复现缺陷的回归测试应能区分旧实现与修复。测试同时包含成功路径和必要的拒绝路径。

## Code Review Rules

### 范围与证据

审查改动是否以足够简单的方式正确实现当前能力，而不是比较谁能想出更多理论反例。缺陷意见应说明运行前提、实际可达或已承诺支持的路径、违反的约束、具体后果。清楚的代码分析可以构成证据，不要求问题先在线上发生；缺少当前调用方也不能否定公开协议或外部可达风险。

分别判断现象是否成立、是否违反已确认要求、建议修法是否必要。现象成立不自动推出某种修法必须采用。区分真实缺陷、设计质疑、可选增强和个人偏好；质疑已确认取舍时说明哪个前提不成立及后果，不把新需求包装成 bug。需要改变 Scope、协议语义或验收时，交给维护者决定。

真实的正确性、安全性、兼容性或正常运行故障不能因“简单优先”被降级。不限制 finding 数量，不伪造无问题结论。

### 根因与复杂度

同一根因在多个路径出现时尽量归并，指出共同责任层和受影响路径。既检查遗漏和缺陷，也检查重复规则、职责错位、补丁叠加、正常能力被拦截和不必要的通用化。复杂度问题同样要有具体影响，不能变成另一种风格争论。

局部修复足够就直接修复；需要统一处理时，不要逐个反例追加特判。横向检查与根因相关的路径，不借机要求无关重构。不要因“防御性代码”这一标签默认接受复杂实现，也不要为了变短删除必要保证。

### 协议与完成标准

先核对本任务涉及的架构、协议、运行前提及维护者决定。技术约束不自动等于某一种实现机制；当前文档明确要求的机制若需调整，应指出迁移需要的决定，而不是忽略文档或继续无限增强。尊重 Bridge、Service 和 Service Script 的职责分配。

机械格式检查留给对应工具；非阻塞建议不自动变成合并条件。Review 无 findings 不等于自动测试、真实浏览器验证或验收已完成。结论涉及未决前提时标明条件；达到当前任务验收后停止扩展，不把整个项目未来的能力都加到当前 PR。

## 验证与交付

验证入口以 [package.json](package.json) 和[开发与 POC](docs/development.md)为准：

- `npm test`：Node 单元与集成测试，当前等价于 `node --test`。
- `npm run poc`：真实浏览器端到端 POC，使用独立测试 Profile 和本地测试页面。

按修改范围选择验证。纯文档改动检查引用、约束一致性和 diff，不无条件重跑浏览器 POC；涉及执行 world、权限、Service Worker 生命周期或 Chrome API 行为的修改，需要相应真实浏览器证据，不能用 mock 通过代替。明确记录提交、环境、已运行结果及未运行项目；不伪装 PASS，不依赖用户日常 Profile 或真实业务账号做自动验收，不提交凭证或敏感浏览器数据。

核实 Review 意见后按共同根因修复、补相关测试并同步必要文档，不按 P1/P2/P3 标签机械增加代码。前提不成立时可用证据回应而不改生产代码；实质争议仍需明确交接。相同根因多轮反复或补丁持续增加例外时，重新审视职责与实现，不无限重试，也不靠忽略真实缺陷收尾。

同一 PR 分支的写入由一个执行者协调。一轮相关代码、测试和文档完成后统一送审，不逐个中间提交催审；审查运行时不重复触发，结果核对 reviewed commit 与当前完整 head。基础设施失败先诊断，不乱改技术逻辑，也不把失败或空 checks 当作审查通过。

交付前更新 PR 最终摘要：能力或文档变化、必要改动理由、当前 head、测试与 Review 实际版本及结果、剩余限制和相关待决记录。旧版本结果不冒充当前结果，执行者自查不冒充独立验证；历史评论保留，简单任务几句话即可。没有明确授权，不自动 merge、部署、操作用户浏览器环境或删除真实数据。

完整完成某个 Issue 的 PR 在正文使用 `Closes #编号`（或等效 Fixes / Resolves）；部分交付只普通引用并说明剩余范围。交付和合并前核对仓库、Issue、目标分支及关闭关联；普通编号引用不等于关闭关联。自动关闭以合并到默认分支且仓库启用该行为为前提，不授予自动 Merge 权限，也不替代 Review 与验收。

合并后负责交接者核实 Issue 的实际状态。遗漏自动关闭时，有授权者确认范围已完成后新增收尾评论并以 Completed 关闭，无授权则报告；不为了清空列表把未完成事项标成完成。

## 决策交接与完成标准

普通实现取舍在已确认要求内自主决定，不逐项请批。影响本次 Scope、接口语义、职责、保障范围或验收且现有依据不足的事项，才交给 Maintainer。

**Issue 正文维护当前目标、Scope、验收和必要摘要；重要问题、维护者决定、阻塞变化、阶段交付及验收结论，由有写入授权的交接者新增评论记录，不能只改正文或旧评论传递进展。** 决定变化时新增评论说明替代哪项旧决定，保留原讨论；正文同步简短摘要和对应评论链接，不在正文、PR 和评论分别维护多套完整决定。错字和格式可以在分支文档或相应讨论中直接修正；无实质变化不刷状态评论，仓库文件仍遵守 Issue → PR 入口。

普通代码缺陷和修复在 PR 对应线程讨论。从 PR 发现维护者待决或需要协助的事项，由发现者回贴关联 Issue 并链接线程；说明依据或歧义、建议与理由、影响和阻塞范围、当前实现是否依赖该选择。Coordinator 整理成可直接决定的问题，不只列“请确认”，不要求固定长模板。

决定前不受影响的工作继续；必要的候选验证须标明未获批准，不能据此扩大正式实现。沉默不等于同意，候选实现、测试通过和 Review 无 Finding 均不代替维护者决定或完整验收。

维护者给出明确结论或有范围的探索授权后，获授权的 Coordinator 在 Issue 新增评论记录。Implementer 实际读取并落实必要文档、代码和测试；Reviewer 核对决定落实与验证结果，再由获授权的交接者新增评论更新该项状态。不只留在聊天里，也不让 Reviewer 代作决定。

交付区分实现、Review、验收、合并状态。影响验收的事项仍待决定或待落实时如实标记；与本次验收无关的可选增强不阻塞交付，也不强迫维护者逐项回答。

## 各类 Agent 的接入

本文件是共享工程原则的单一维护位置；Issue、PR 和启动提示引用它，不复制多套规则。支持自动加载 AGENTS.md 的工具使用本文件；其他 Agent / runner 启动时显式读取并传入。委派子任务传递同一规则版本、相关 Contract、本次职责和授权。

启动消息只需指向仓库与任务；具体条件由 Issue / PR 和授权记录承载，不另用长提示维护第二套规则。简要标明本次仓库/ref 与规则来源，不只回复“已理解”。

规则更新后，已有会话、旧分支和不自动加载文件的 runner 需要显式重读或更新上下文。文件存在不代表所有参与者已经收到；未合并规则仅适用于包含该文件或被明确传入该版本的任务，不宣称 main 或所有外部 Agent 已自动生效。
